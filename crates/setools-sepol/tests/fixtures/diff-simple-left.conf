class infoflow

sid kernel
sid security

common infoflow
{
	read
	write
}

class infoflow
inherits infoflow

sensitivity s0 alias { shared_sens_alias removed_sens_alias };
sensitivity removed_sensitivity;
dominance { s0 removed_sensitivity }

category c0 alias { shared_category_alias removed_category_alias };
category c1;
category removed_category;
level s0:c0.c1;
level removed_sensitivity:c0.c1;

mlsconstrain infoflow read (l1 domby l2);

attribute changing_attr;
attribute removed_attr;
type system, changing_attr;
type left_member, changing_attr, removed_attr;
type right_member;

role system;
role system types system;

bool same_bool true;
bool removed_bool true;
bool modified_bool false;

allow system self:infoflow read;
policycap network_peer_controls;

user system roles system level s0:c0 range s0:c0 - removed_sensitivity:c0.c1;

sid kernel system:system:system:s0:c0
sid security system:system:system:removed_sensitivity:c0.c1
