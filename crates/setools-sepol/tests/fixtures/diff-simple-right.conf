class infoflow

sid kernel
sid security

common infoflow
{
	read
	write
}

class infoflow
inherits infoflow

sensitivity s0 alias { shared_sens_alias added_sens_alias };
sensitivity added_sensitivity;
dominance { s0 added_sensitivity }

category c0 alias { shared_category_alias added_category_alias };
category c1;
category added_category;
level s0:c0.c1;
level added_sensitivity:c0.c1;

mlsconstrain infoflow read (l1 domby l2);

attribute changing_attr;
attribute added_attr;
type system, changing_attr;
type left_member;
type right_member, changing_attr, added_attr;

role system;
role system types system;

bool same_bool true;
bool added_bool true;
bool modified_bool true;

allow system self:infoflow read;
policycap always_check_network;

user system roles system level s0:c0 range s0:c0 - added_sensitivity:c0.c1;

sid kernel system:system:system:s0:c0
sid security system:system:system:added_sensitivity:c0.c1
