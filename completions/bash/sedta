# Generated by `cargo run -p setools-xtask -- generate`; do not edit.
_setools_rs_sedta()
{
    local cur prev
    COMPREPLY=()
    cur="${COMP_WORDS[COMP_CWORD]}"
    prev="${COMP_WORDS[COMP_CWORD-1]}"

    case "$prev" in
        -p|--policy|-o|--output_file) COMPREPLY=( $(compgen -f -- "$cur") ); return 0 ;;
        -s|--source|-t|--target|-A|--all_paths|-l|--limit_trans) return 0 ;;
    esac

    if [[ "$cur" == -* ]]; then
        COMPREPLY=( $(compgen -W '-h --help --version -p --policy -s --source -t --target --full --stats -v --verbose --debug -S --shortest_path -A --all_paths -r --reverse -l --limit_trans -o --output_file --json' -- "$cur") )
    else
        COMPREPLY=()
    fi
}
complete -F _setools_rs_sedta sedta
