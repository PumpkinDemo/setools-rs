# Generated by `cargo run -p setools-xtask -- generate`; do not edit.
_setools_rs_sechecker()
{
    local cur prev
    COMPREPLY=()
    cur="${COMP_WORDS[COMP_CWORD]}"
    prev="${COMP_WORDS[COMP_CWORD-1]}"

    case "$prev" in
        -o|--output_file) COMPREPLY=( $(compgen -f -- "$cur") ); return 0 ;;
    esac

    if [[ "$cur" == -* ]]; then
        COMPREPLY=( $(compgen -W '-h --help --version -o --output_file -v --verbose --debug --json' -- "$cur") )
    else
        COMPREPLY=( $(compgen -f -- "$cur") )
    fi
}
complete -F _setools_rs_sechecker sechecker
