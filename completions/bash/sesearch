# Generated by `cargo run -p setools-xtask -- generate`; do not edit.
_setools_rs_sesearch()
{
    local cur prev
    COMPREPLY=()
    cur="${COMP_WORDS[COMP_CWORD]}"
    prev="${COMP_WORDS[COMP_CWORD-1]}"

    case "$prev" in
        -s|--source|-t|--target|-c|--class|-p|--perms|-x|--xperms|-D|--default|-b|--bool) return 0 ;;
    esac

    if [[ "$cur" == -* ]]; then
        COMPREPLY=( $(compgen -W '-h --help --version -v --verbose --debug -A --allow --allowxperm --auditallow --auditallowxperm --dontaudit --dontauditxperm -T --type_transition --type_change --type_member --role_allow --role_transition --range_transition -s --source -t --target -c --class -p --perms -x --xperms -D --default -b --bool -eb -ep -ex -Sp -ds -dt -rs -rt -rc -rd -rb --json' -- "$cur") )
    else
        COMPREPLY=( $(compgen -f -- "$cur") )
    fi
}
complete -F _setools_rs_sesearch sesearch
